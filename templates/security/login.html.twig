{% extends 'base.html.twig' %}

{% block title %}
    {% trans %}Log in!{% endtrans %}
{% endblock %}

{% block body %}
    <form method="post">
        {% if error %}
            <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
        {% endif %}

        {% if app.user %}
            <div class="mb-3">
                {{ 'You are logged in as %userIdentifier%'|trans({'%userIdentifier%': app.user.userIdentifier})}},
                <a href="{{ path('app_logout') }}">Logout</a>
            </div>
        {% endif %}

        <h1 class="h3 mb-3 font-weight-normal">{% trans %}Please sign in{% endtrans %}</h1>
        <label for="username">{% trans %}Email{% endtrans %}</label>
        <input type="email" value="{{ last_username }}" name="_username" id="username" class="form-control" autocomplete="email" required autofocus>
        <label for="password">{% trans %}Password{% endtrans %}</label>
        <input type="password" name="_password" id="password" class="form-control" autocomplete="current-password" required>

        <input type="hidden" name="_csrf_token"
               value="{{ csrf_token('authenticate') }}"
        >

        <button class="btn btn-lg btn-primary" type="submit">
            {% trans %}Sign in{% endtrans %}
        </button>
    </form>

    <p><a href="{{ path('app_forgot_password_request') }}">{% trans %}Password forgotten{% endtrans %}?</a></p>

    <p>{% trans %}You don't have an account yet?{% endtrans %}
        <a href="{{ path('app_register') }}">{% trans %}Registration{% endtrans %}</a>
    </p>
{% endblock %}
