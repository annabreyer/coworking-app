{# @var ea \EasyCorp\Bundle\EasyAdminBundle\Context\AdminContext #}
{# @var field \EasyCorp\Bundle\EasyAdminBundle\Dto\FieldDto #}
{# @var entity \EasyCorp\Bundle\EasyAdminBundle\Dto\EntityDto #}

{% if entity.instance.payments is empty %}
Keine Zahlungen vorhanden
{% else %}
    {% for payment in entity.instance.payments %}
        {{ (payment.amount/100)|number_format(2, ',', '.') }} € | {{ payment.date|date('d.m.Y') }} | {{ payment.type|trans }} {% if payment.comment is not empty %}({{ payment.comment }}){% endif %}<br>
    {% endfor %}
{% endif %}
